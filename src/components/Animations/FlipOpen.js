import React, { useRef } from 'react';
import { motion, useScroll } from 'framer-motion';
import Section from '@components/Layout/Sections';

export default function FlipOpen({ children }) {
	return <Section></Section>;
}
